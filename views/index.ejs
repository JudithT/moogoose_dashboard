<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h2>Hey Mongoose! Find out about your tribe!</h2>
    <p>There are several types of Moongoses. Below is a list:</p>

  <a href="/mongooses/new"><button type="button" name="button">Add a new Moongoose!</button></a>
    <% for (var x in users) { %>

      <a href="/mongooses/<%= users[x].id%>"><h3>Name: <%= users[x].name %></h3></a>
        <h4>age: <%= users[x].age %></h4>
        <h4>color: <%= users[x].color %></h4>
        <h4>weight: <%= users[x].weight %></h4>
        <h4>gender: <%= users[x].gender %></h4>
        <h4>id: <%= users[x].id%></h4>




        <hr>
        <% } %>
  </body>
</html>
